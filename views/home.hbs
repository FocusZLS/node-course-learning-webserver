<!DOCTYPE html>
<html>
    {{> header}}
    <body>
        {{> navbar}}
        <div class="container">
            <h1>{{pageTitle}}</h1>
            <p>{{screamIt welcomeMessage}}</p>
            <button class="btn waves-effect waves-light">Nice</button>
        </div>
    </body>
    {{> footer}}
</html> 